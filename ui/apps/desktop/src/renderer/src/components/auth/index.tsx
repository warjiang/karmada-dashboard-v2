// Re-export from DesktopAuthProvider for compatibility with dashboard code
// that imports from @/components/auth
export { useAuth } from '../DesktopAuthProvider'
export { default } from '../DesktopAuthProvider'
